<!--index.wxml-->
<!-- <view class="container">
  <view class="userinfo">
    <button wx:if="{{!hasUserInfo && canIUse}}" open-type="getUserInfo" bindgetuserinfo="getUserInfo"> 获取头像昵称 </button>
    <block wx:else>
      <image bindtap="bindViewTap" class="userinfo-avatar" src="{{userInfo.avatarUrl}}" mode="cover"></image>
      <text class="userinfo-nickname">{{userInfo.nickName}}</text>
    </block>
  </view>
  <view class="usermotto">
    <text class="user-motto">{{motto}}</text>
  </view>
</view> -->

<!-- <view class="title-card" wx:if="{{user.id_admin}}"> -->
<view class="container">
  <view class="holiday-overview"  wx:for="{{holidays}}" wx:for-item="holiday">
    <view class="holiday-card">
      <view class="title">{{holiday.name}}</view>
      <view>📍  {{holiday.location}}</view>
      <view>📅Start Date: {{holiday.startDate}}</view>
      <view>📅End Date: {{holiday.endDate}}</view>
    </view>
  </view>
</view>